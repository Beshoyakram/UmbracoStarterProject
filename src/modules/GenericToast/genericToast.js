
// import { createApp } from 'vue';
// import GenericToast from './GenericToast.vue';

// export default function load() {
//     const selector = 'generic-toast';

//     // Is the custom Vue root element in the DOM?

//     if (!document.querySelector(selector)) {
//         return;
//     }

//     // Create a new Vue app with the imported component

//     const app = createApp(GenericToast, {
//         props: { ...window[selector] }
//     })
//     app.mount(selector)
// }
